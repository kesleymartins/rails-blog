<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>
  <h5>Created by: <%= post.user.email %></h5>
  <p><%= pluralize(post.views, 'view')%></p>
  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

</div>
